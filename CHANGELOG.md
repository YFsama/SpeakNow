# 更新记录

## v0.4.7（2026-09-11）—— 修复思考型模型「润色永远输出原文」+ 悬浮窗边缘重制 + 标点确定性规整

### 修复

- **思考型模型润色/纠错永远输出原文（核心修复）**：GLM-4.5/4.6 等思考型模型经 OpenAI 兼容接口调用时深度思考默认开启，且思考 token 计入 `max_tokens` 额度；原预算公式 `400+2×字数`（典型仅 460~700）远低于智谱官方建议的 1024 下限——思考直接烧光预算（`finish_reason=length`），正文一个字都不产出，流水线静默回退原始转写。表现为：悬浮窗「深度思考中」里明明能看到模型想出修复，最终输出的却永远是没修过的原文。现在：① `max_tokens` 下限提至 1024；② 流式解析捕获 `finish_reason` 与思考痕迹，判定「烧尽」后自动重试一次（思考型模型关思考 + 4096 预算，普通模型撞上限只放大预算）；③ 重试仍空不再静默吞掉——悬浮窗明示「AI 未返回正文，使用原始识别结果」并写入 pipeline 诊断日志
- **悬浮窗阴影边缘断层**：单层 70px 大模糊阴影的可视延伸约 48px，而窗口两侧仅 10px 余量——阴影被透明窗口的矩形边界硬切成锐利切线（浅色桌面尤其刺眼），叠加低透明长尾经 8bit 通道量化在部分屏幕形成同心色带。现在窗口加宽至 560/600（两侧各留 30px 完整渐隐空间），边缘呈现整体重制：1px 勾边 + 接触/近景/环境三层短焦分层阴影（衰减 26px 内完成，量化步进不可感知）+ 与描边同色系的阶段色微光（取代浑浊的暗灰晕）+ 外层羽化遮罩把任何越界阴影沿窗口边缘渐隐归零——无论 DPI、壁纸、缩放如何，不会再出现硬切边或色带
- **界面缩放裁切悬浮窗**：界面缩放（85%~130%）经 `body.zoom` 放大卡片但窗口尺寸不变，超过 104% 时卡片本体被窗口边缘裁切。窗口尺寸现随缩放同步放大，Rust 侧定位与工作区夹紧也按缩放后的实际尺寸计算
- **分段拼接处标点缺失**：流式 VAD 切段与长音频分段此前对中文用空字符串直接拼接——「今天讨论预算」+「下午继续」粘成一句，分段处标点永远缺失。现按边界智能衔接：两侧已有标点原样相连，拉丁字符边界补空格（`End.` + `Next` 的空格保真），中文边界补「，」（静音切分处本就是语义停顿）；四处拼接点（实时字幕、流式汇总、云端分段、MiMo 分段）统一收敛

### 改进

- **思考快路径（首字延迟优化）**：纠错/润色/自定义指令模式下，GLM/Qwen3 系思考模型首轮请求即关闭深度思考——思考对这类短文本机械修正收益甚微，却把首字延迟拖到数秒且随时烧尽预算；首字从此亚秒级、正文必出。编程指令模式（需结构整理）保留思考；未知模型不带任何附加参数，行为不变；附加参数被严格校验未知字段的网关拒绝（4xx）时自动去参重发一次
- **确定性标点/排版规整**：紧邻汉字的半角标点（`,` `;` `:` `?` `!`）全角化、汉字与 ASCII 字母数字之间补空格（盘古之白），改为输出前确定性执行——对 LLM 输出与快速模式原文一视同仁，不再依赖模型自觉。句点永不转换（小数点、版本号零误伤），纯英文、URL、时间等场景零改动

## v0.4.6（2026-09-04）—— 润色质量深优化 + macOS 官方构建 + 麦克风增益自动化

### 新增

- **概括删减守卫**：关键词守卫只能比对英文词元，纯中文内容被模型概括/删减时无从察觉——新增长度比例闸：中文为主的转写在「仅纠错」模式输出不足原文 75%、「纠错 + 润色」不足 55% 即判定概括，并入定点修补（提示「说了几件事就输出几件事，条件与否定关系一个都不能丢」）。英文口语（填充词比例天然偏高）、编程指令模式（条目化合理收缩）、短文本不检测，避免误报
- **macOS 官方构建**：CI 升级为双平台矩阵——`check` 在 Windows + macOS 上跑全套类型检查 / 编译 / 单测（此前只在 Windows 检查，macOS 编译问题无门禁拦截）；`publish` 发版时同时构建 **macOS Apple Silicon（aarch64）DMG** 与 Windows NSIS 安装包并附到同一 Release——Mac 用户不再需要自行编译，直接下载即用
- **macOS 使用文档**（`docs/macOS.md`）：Gatekeeper 绕过（未签名产物的右键打开 / `xattr -cr`）、辅助功能与麦克风权限授权说明、源码构建步骤、平台能力差异表
- **关键词对齐守卫（三层递进）**：针对「润色后关键词不是本意」的核心痛点——模型擅自翻译、替换或漏掉原文关键词（如把 Tauri 写成「桌面框架」、丢掉版本号 4.6）时依次处置：① 术语表声明了误识映射（如 `Rust=拉斯特`）且输出出现误识形式时，直接确定性替换为零成本必生效修复；② 仍有丢失则发起**定点修补**重试——请求带上原始转写与上一次输出、只要求把关键词修回原样其余一字不改（比让模型从同样输入重新生成有效得多，小模型重新生成往往原样再错），流式路径以「思考中」提示「⚠ 定点修补中…」；③ 择优采纳——修补版守卫违规更少才采用，否则保留原版，重试绝不劣化结果。检测规则：原文中的英文词元（含 ≥2 位数字的版本/编号）与术语表词条必须出现在输出中；否定/量化词（no/not/all…）刻意不豁免；ok/yeah/code/app/web/demo/ppt 等填充与可汉化借词不误报；自定义指令（翻译等）模式不启用
- **术语表支持误识声明**：每行可写 `Rust=拉斯特|拉斯`——「=」右侧是该术语的典型 ASR 误识形式，提示词中点名「原文出现「拉斯特」等写法时必须写作「Rust」」，比泛泛列出有效得多；守卫同样强制执行（原文命中误识形式、输出却无规范写法时触发重试）
- **ASR 热词联动**：用智谱 GLM-ASR 时，「AI 优化」页术语表的规范写法自动并入识别热词（去重、上限 100），专有名词在识别源头就倾向正确写法，比事后靠 LLM 纠正更可靠
- **增益按设备记忆**：软件增益不再全局一份——每个输入设备（含「系统默认」模式）独立记忆，切换麦克风自动换入对应增益，互不干扰；设备卡片上直接展示各设备记忆的增益徽章
- **录音自动增益（AGC）**：录音中实时监测电平，「确有语音」但音量过小（窗口峰值低于目标）时每 1.5 秒 +3dB 逐步提升，接近削波快速回落。只在增益前信号高于语音门限时才放大，绝不泵底噪；调节范围以用户设定的基准增益 ±12dB 为界。学到的增益在录音结束后按设备写回记忆（`sn-gain-learned` 事件），下次录音直接从学到的值起步，不再重复爬升。麦克风页可开关，默认开启
- **新设备首用自动校准**：切换到没有增益记忆的麦克风时，若 AGC 开启则后台自动校准一次（对麦克风说话即可），校准结果按设备保存
- **审阅窗口重新优化升级**：结果流式逐字回填编辑框（不再黑盒等待）；可临时切换优化模式（仅纠错 / 纠错 + 润色 / 编程指令），润色过头当场改用「仅纠错」重跑；新增「恢复原文」一键回到原始转写；失败原因直接显示在窗口内，不再静默吞掉
- **流式中止**：AI 优化期间开始新录音时立即中止旧请求并跳过旧结果，不再白白耗费流量与 token

### 改进

- **三种优化模式提示词重写（小模型友好）**：加入少样本示例（输入/输出对，含「无错则原样输出」的反例锚点），本地 4B 级模型行为显著收敛。「纠错 + 润色」新增强制底线：人名/产品名/术语/英文缩写一字不改（只许修拼写与大小写，禁止翻译替换）、数字/版本号/路径/命令原样保留、否定与条件关系一个不能丢；「仅纠错」强调「替换词须与原词读音接近，宁可少改不可改错」；「编程指令」补充示例；仅纠错模式温度降至 0.1
- 增益在音频回调内每块重新读取（原子量），支持录音期间实时调节，无需重建音频流；「自动校准」与增益滑杆的改动均写入当前设备的记忆，增益区块标注当前生效设备
- 修正配置注释：软件增益实际上限为 45dB（校准建议上限 40dB）
- **自定义指令修复**：此前自定义指令与模式系统提示互相打架（如「翻译成英文」仍被告知「输出语言与原文一致」），现改用中性系统提示，翻译/改写类指令真正生效
- 长文本优化 `max_tokens` 上限 4096 → 8192；编程指令模式（整理为条目会膨胀）给 3 倍余量

### 修复

- **llama-server 孤儿进程（约 10GB 内存泄漏）**：本地引擎此前只靠正常退出路径收尾——应用被强杀/崩溃一次就会遗留常驻 ~10GB 提交内存的 llama-server；且被「接管」的无句柄实例连正常退出都收不掉，曾实际遗留进程运行 8 天，叠加系统其他内存大户直接把虚拟内存耗尽（白屏/卡死的帮凶）。现在：① 子进程挂入 Job Object（KILL_ON_JOB_CLOSE），应用无论怎么退出（含强杀/崩溃）都会被内核连带收掉；② 每次启动清理 runtime 目录内的遗留 llama-server（按可执行文件路径精确匹配，不误伤用户自跑的同名进程）；③ 识别引擎从本地切到云端时立即停掉常驻引擎（用回本地时按需自动拉起）
- **流式中文乱码隐患**：SSE 改为按字节缓冲、按完整行解析——多字节 UTF-8 字符被网络分块拦腰截断时不再产生替换字符混进正文
- **思考型模型推理混入正文**：本地 qwen3 / R1 蒸馏版等把 `<think>…</think>` 混进 content 时自动剥离（含流被截断的未闭合情形）

## v0.4.5（2026-08-24）—— 管理员终端支持 + 麦克风选择修复

### 新增

- **一键以管理员身份重启**：悬浮窗报「管理员身份运行」错误时可直接点「🛡 以管理员身份重启」；设置 · 输出页新增「运行权限」区块（含当前权限状态）。以管理员运行的终端（Windows Terminal / PowerShell）会拒绝普通权限程序的按键注入（UIPI），SpeakNow 需同样提权才能自动输入。实现上经中继子进程重启，避开单实例弹回
- **UIPI 报错带进程名**：如 `目标窗口 WindowsTerminal.exe 以管理员身份运行…`，一眼定位是哪个窗口拦截

### 修复

- **麦克风选择「重启丢失」**：设备列表此前只在启动时拉取一次，无线接收器（如 ROG 2.4GHz）休眠/重枚举/刚开机未就绪时，已保存的麦克风在列表中消失、看似配置丢失。现在①已保存但未检测到的设备置顶显示「就绪后自动恢复」；②设备列表每 10 秒及窗口聚焦时自动刷新；③录音时设备名模糊匹配（忽略空格/大小写差异）；④设备缺位时回退系统默认继续录音（常与所选设备相同），不再整次失败
- `pipeline.log` 的录音开始行带上所用设备名，便于核对实际采集麦克风

## v0.4.4（2026-08-24）—— 模型吞吐观测

### 新增

- **ASR 实时率**：`pipeline.log` 识别行新增音频时长与实时率（如 `音频 3.2s，实时率 7.4×`）；悬浮窗识别耗时后同样显示 `· 7.4×`
- **AI 生成速度**：净生成吞吐（扣除首字等待后的字/秒），日志与悬浮窗同步展示（如 `生成 1490ms，35字/s`）；中文约 1 字 ≈ 1 token，可直接当 token/s 参考
- `sn-result` 事件载荷新增 `llmFirstMs` / `audioSecs`

## v0.4.3（2026-08-24）—— 右键粘贴终端支持

### 新增

- **终端键入模式**（粘贴按键新选项）：终端窗口的单行文本直接**模拟键入**逐字打出，不依赖任何粘贴快捷键——适配 Windows Terminal / PuTTY 等只用鼠标右键粘贴的终端；多行文本自动退回 Shift+Insert（避免逐行触发命令执行），非终端窗口照常 Ctrl+V
- **被顶掉的结果不再丢失**：AI 优化耗时较长、结果被下一次听写取代时，自动把文本复制到剪贴板并提示「已被新录音取代 · 结果已复制」，右键 / Ctrl+V 手动粘贴即可

## v0.4.2（2026-08-24）—— 延迟观测

### 新增

- **延迟记录**：每次听写在 `pipeline.log` 落一行可对比的耗时——`耗时对比｜识别 Xms｜优化 Yms（首字 Zms）｜合计 Nms`，另含引擎/模型与字数
- **流式首字延迟**：AI 优化的首个正文 token 耗时单独记录（流式体验的核心指标）
- 悬浮窗完成态在「识别 / 优化」之外新增「合计」耗时；历史页原有的分项耗时保持不变

## v0.4.1（2026-08-24）—— 终端粘贴键与语气词标点修复

### 修复

- **WSL / Windows Terminal 粘贴失效**：自动模式对 Unix 风格终端（WT / WSL / mintty / Alacritty / WezTerm）改发 `Ctrl+Shift+V`（其标准粘贴绑定），传统 conhost 与 PuTTY 仍用 `Shift+Insert`；设置页新增「Ctrl + Shift + V（终端风格）」手动选项
- **语气词清理遗留标点**：移除语气词后留下的「，。」「，！」等混合标点现在收敛为一个（句末级优先）；句首孤悬标点清除，句尾逗号去掉、句号保留；正常文本的标点与空格不受影响

## v0.4.0（2026-08-24）—— 全链路流式体验

### 新增

- **AI 优化流式逐字输出**：纠错/润色改为 SSE 流式，结果在悬浮窗中逐字打出（带光标），无需等完整响应；思考型模型（如 MiMo）的推理过程以暗色小字先行展示，正文就绪后无缝切换
- 接口不支持流式或流式中断时自动回退非流式请求，行为无感兜底
- 悬浮窗 AI 优化阶段重新设计：流式结果卡片（自动滚动跟随最新内容）+ 底部一行原文参照
- 识别侧启用「边说边出字」分段流式：说话期间按停顿实时上屏字幕，说完即出全文

## v0.3.1（2026-08-24）—— 录音提前结束与说话中插入修复

### 修复

- **录音提前结束**：热键 toggle 加 300ms 去抖——键盘连击/驱动重复会在几十毫秒内触发第二次 toggle，造成「一开就停」（话没说完录音就结束）或幽灵录音，进而引发旧结果晚到输入
- **说话时旧结果突然被输入**：粘贴前新增麦克风活动探测（约 220ms 采样，说话峰值超过阈值即暂缓，最长等 6 秒）——即使录音意外早停、未重新按键继续说话，旧结果也不会插进你正在说的话里
- 新增 `pipeline.log` 链路追踪：热键 toggle / 录音开始与结束（热键 / VAD / 最长时长 / 设置页）/ 结果过期跳过 / 输入结果全部带时间戳记录，时序类问题可精确诊断

## v0.3.0（2026-08-24）—— 稳定性与可靠性

聚焦修复三类"用着用着出问题"的疑难杂症。

### 修复

- **设置自动保存卡死**（「正在自动保存…」永不消失）
  - 根因：保存时要重注册全局快捷键，该操作需经主线程派发；而麦克风相关的 WASAPI/COM 命令恰好也跑在主线程上，无线麦/蓝牙设备响应慢时主线程被占，保存随之挂起
  - 修复：快捷键配置未变化时跳过重注册（绝大多数保存 <5ms）；变化时改后台线程注册；设备枚举、系统音量、硬件增益、开始/停止录音等命令全部移出主线程
  - 前端加保存看门狗：15 秒超时止损、自动重试、保存期间的新改动自动补存
- **WSL / 终端无法自动输入**
  - 等待用户物理松开热键修饰键（Ctrl+Shift 残留会让注入的 V 变成 Ctrl+Shift+V，终端不识别）
  - 粘贴按键新增「自动」模式（默认）：检测到 Windows Terminal / WSL / conhost 等终端窗口自动改用 Shift+Insert
  - 等待前台焦点真正回到目标窗口后再发键；目标窗口以管理员运行（UIPI 拦截注入）时明确报错而非无声失败
- **旧句子晚到插入新内容**（说完新话，上一句突然冒出来）
  - 会话代数守卫原仅覆盖到 AI 优化前，现在粘贴全链路三处复查；被新录音取代的旧结果明确提示「已跳过」并保留进历史
  - 等待循环内置提前退出（新录音一开始，旧粘贴流程立即放弃）
  - 粘贴全局串行化，杜绝两次听写交叉操作剪贴板
- **配置文件丢失（API Key 需重填）**
  - 根因：配置为原地覆写，进程被强杀会留下半截 JSON，下次启动静默回退默认值并被落盘固化
  - 修复：原子写入（临时文件 + rename）；每次保存前自动备份 `config.bak`；主文件损坏时自动恢复并留档 `config.corrupt`
- **悬浮窗长文本渲染**：窗口高度按内容自适应（最高 560px），结果区可滚动阅读，正确保留换行
- **剪贴板粘出旧内容**：写入校验升级为「读回一致 + 剪贴板序号前移」双重验证，约 2 秒内重试；校验不过宁可提示手动粘贴也绝不自动粘出旧内容

### 其他

- 设置保存链路追踪日志（`save-trace.log`），异常可自诊断
- 历史记录同样改为原子写入

## v0.2.0（2026-08）—— 识别引擎与效率

### 新增

- **Qwen3-ASR 1.7B 本地离线引擎**（llama.cpp）
  - 普通话字错率约 5%，GPU 单句延迟约 0.4 秒；模型约 2.4GB，HuggingFace 官方源直连一键下载
  - 自动检测显卡选 Vulkan/CPU 构建，Vulkan 失败自动回退 CPU；应用启动后台预热，快捷键秒级响应；异常残留进程自动接管
- **边说边出字（流式分段识别）**：按停顿自动分段，字幕实时上屏
- **语气词清理**：自动去除「嗯 / 呃 / yeah」等口头音
- **光标跟随悬浮窗**：毛玻璃卡片自动跟随输入光标，词级 diff 高亮，悬停暂停阅读
- **预览编辑模式**：输入前可编辑、重新 AI 优化再确认
- **麦克风深度调校**：系统音量 / 硬件 dB 增益（Mic Boost）直接读写、全设备同测、自动增益校准、Windows 麦克风隐私自检
- **本地模型管理**：模型卡展示下载/运行时状态，支持删除释放磁盘
- **NSIS 安装包**（简体中文、当前用户安装、免管理员）

## v0.1.0（2026-08）—— 首个版本

核心语音输入链路。

- 全局快捷键（默认 `Ctrl+Shift+Space`），按住说话 / 按一下开关两种模式，可改绑
- 快速模式第二快捷键（跳过 AI 直接输出）
- ASR：MiMo 云端、GLM-ASR / Groq / OpenAI 等云端 API、本地 Whisper（tiny/base/small）
- AI 纠错与优化：仅纠错 / 纠错 + 润色 / 优化为编程提示词；支持本地 Ollama / LM Studio
- VAD 静音自动结束、ASR 热词、自动粘贴（剪贴板还原 / 模拟键入）、自动回车
- 历史记录（50 条，搜索 / 对比 / 重新优化 / 导出）、使用统计
- 系统托盘、开机自启、声音反馈、深浅主题、误触忽略
